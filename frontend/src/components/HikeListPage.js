import React from 'react';
import { Link } from 'react-router-dom';
import { withRouter } from 'react-router';

const HikeListPage = () => {
  return (
    <div>
      <h4>
        Hike List Page
      </h4>
    </div>
  );
};

export default withRouter(HikeListPage);
